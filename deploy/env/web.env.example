# Production web env — copy to web/.env.production before build
PORT=3001
NODE_ENV=production

# Public site URL (no trailing slash)
NEXT_PUBLIC_SITE_URL=https://downloadvideos.work.gd

NEXT_PUBLIC_API_URL=https://downloadvideos.work.gd/api
